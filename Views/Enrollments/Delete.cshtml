@model Enrollment
<h1>Delete Enrollment</h1><p>Delete enrollment for <strong>@Model.Student?.FullName</strong> in <strong>@Model.Subject?.Name</strong>?</p>
<form asp-action="Delete"><input asp-for="Id" type="hidden"/><button class="btn btn-danger" type="submit">Delete</button> <a asp-action="Index">Cancel</a></form>
